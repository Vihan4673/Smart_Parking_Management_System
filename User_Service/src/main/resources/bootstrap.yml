spring:
  application:
    name: User-Service

  cloud:
    config:
      discovery:
        enabled: true
        service-id: Config_Server
      fail-fast: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
